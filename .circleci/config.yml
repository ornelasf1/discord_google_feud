version: 2.1
jobs:
  build:
    docker: 
      - image: cimg/base:2020.01
    steps:
      - run:
        name: "Deploy app"
        command: |
          git pull origin master
          sudo systemctl restart gfeud
